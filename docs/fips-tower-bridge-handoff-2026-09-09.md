IMPORTANT MANAGER SCOPE CORRECTION: You are READ-ONLY independent reviewer. Another worker 082e14f1-9e73-4d5d-b694-8907995e06a7 owns WMapp implementation. Do not implement/edit source. Review its emerging bridge and Flight Deck integration, test security and WebView feasibility, report findings to linked FD task and own task. Prior implementation instructions below are requirements to REVIEW, not write authority.

# Bridge Flight Deck FIPS requests through WM App without changing browser origin

Task: e0cef3e2-9fb4-46a0-8008-1725f8a54103

Deliver native WMapp bridge required by @[Flight Deck/Tower FIPS](mention:task:b5f96311-d672-46ca-ab2d-e0bb0cdde8f8). Origin @[Implementation request](mention:message:e6272678-db9d-4a0c-af99-deb95cd28585) in @[Wingman Suite](mention:channel:855d79ef-94cd-4661-9f45-f9ce225f69c4), thread 7f8c9eda-6e1a-49fe-b355-821632d340bd. Pete requested full FIPS access now via supervised sub-Autopilots, keeping HTTPS/iPhone access; discovery deferred. Write scope ONLY /Users/mini/code/wm/wmapp. Other workers own Flight Deck, Tower and Autopilot. Read repo instructions and architecture v4 saved scene at /Users/mini/code/wingmanbefree/artifact-wapp/artifacts/Wingman_Suite/wingman-suite-arch/v4/excalidraw-scene.json. Preserve native browser origin, existing Flight Deck Dexie cache and TowerSyncService ownership. Raw HTTP .fips navigation changes IndexedDB origin and loses secure context; direct HTTPS to HTTP .fips fetch is mixed content.

Agreed integration contract to implement and validate: window.wingmanTowerTransport.connect({endpoint: exact http://<npub>.fips:<port> origin, logicalTower: current HTTPS Tower}) returns {version:1, endpoint, proxyBaseUrl: http://127.0.0.1:<port>/<unguessable-paired-route>}. Native proxy pins one manually approved FIPS endpoint; preserves path/query/body/Authorization/HTTP errors and binary response bodies, SSE streaming/cancellation; strips untrusted forwarding/cookie headers; rejects redirects/outside targets; allows only requesting approved Flight Deck Origin via CORS incl preflight. No signing, no public fallback, no arbitrary SSRF/open proxy. Flight Deck signs mesh URL then fetches loopback proxyBaseUrl+path from both page and sync worker. Validate WebView loopback mixed-content/PNA behavior on macOS WMapp rather than weakening global security. Scope capability to trusted first-party Flight Deck page and explicit pairing; untrusted WApps must not mint proxies, steal capabilities or retarget streams. Revoke on navigation/logout/close as appropriate. Unsupported iPhone remains HTTPS with explicit unavailable FIPS capability. If exact contract must change, post concrete change to linked FD task immediately before implementation; do not leave producer and consumer incompatible.

Use main and preserve concurrent work; commit all compatible nonignored tested state with Conventional Commits; no destructive Git or raw secret access. Do not stop/restart Autopilot. Routine WMapp build/activation authorized when needed but do not disrupt unrelated apps. Run meaningful Dart/native proxy/JS bridge tests (target restrictions, Origin, body/auth forwarding, redirects, streams and cancellation), Flutter analyze/test/build per repo, and actual local WebView/mesh smoke when possible. No speculative implementation-only tests. Provide runnable integration evidence and build/package details, not just scaffolding. Report source vs installed binary accurately. Existing managed FIPS apps continue working.

Read latest comments on BOTH this task and linked FD task at start and before handoff. Post transport contract/progress and evidence to both, manager owns chat replies. Use broker helper /Users/mini/wingmen/wingman21/mycode/fips-delivery-pg.mjs if CLI context missing; pg(method,path,body), relative /api/v4/flightdeck-pg. Backend https://sb4.otherstuff.studio, workspace 2e5caefd-dd65-45d2-b747-ee874e8e5fc9. Set goal metadata, next-action reflect while active; stop at real handoff. Keep task in_progress until manager review. Return callback with files, commit, tests and exact runtime limitations. FD worker session is 4425b36f-253a-46ff-aa6c-4fc4ea799793; manager 95c891cb-f3c8-423c-a5b5-44e99f86cdbe; coordinate through task comments if session sends denied.