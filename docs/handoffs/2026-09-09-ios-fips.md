# Implement native iPhone FIPS support in WMAPP

Pete explicitly requested on 2026-09-09: "How would we get FIPS running on iPhone?" and, after the proposal below, "Yes please build this now…". This is implementation, not another design-only report. Goal: bundled Rust FIPS in an iOS Packet Tunnel extension, integrated with WMAPP setup/navigation, built and tested, and a physical-device proof where signing/device availability permits.

Repo/write scope: /Users/mini/code/wm/wmapp. Work on main; preserve concurrent work. Commit all compatible nonignored tested state with Conventional Commits; inspect full diff before commit, never reset/discard unknown changes or expose credentials. No unrelated repo changes. No Autopilot restart. Routine affected app build/install is authorized; do not publish to TestFlight/App Store in this task. If Pete's known connected iPhone is available and signing succeeds, install and launch the new build. Never guess identity/account or access/export secrets. Use broker-aware Flight Deck tools/current Autopilot CLI only.

Origin: @[WMAPP features](mention:channel:d8d00881-ac84-41eb-ab0d-2c2afb77ddf3), thread 3d7d10a6-14b9-4c56-a52b-c946a96eb5c2, @[Build authorization](mention:message:dee5b8d4-b120-40ab-a5dc-f9c7c182ffb6). Workspace 2e5caefd-dd65-45d2-b747-ee874e8e5fc9, scope 76d518f7-c477-4374-bf74-5d36fda570ed. Manager def818b3-6b7a-4382-85f1-cc2a8652c717 owns chat replies; you post task progress/evidence and return a supervised callback. Task ID will be appended before dispatch. Read task/latest comments at start and before handoff. Keep in_progress until manager review. Set session goal and next-action reflect; stop at real handoff with evidence.

Accepted proposal: compile pinned FIPS Rust core for iOS, reuse platform-neutral Android lifecycle/core code where appropriate, provide Swift-compatible boundary; add NEPacketTunnelProvider using public packetFlow APIs (never private utun FD extraction); host app controls NETunnelProviderManager and user VPN consent. Route FIPS mesh IPv6 traffic and .fips DNS with split routing; normal internet remains normal. Preserve exact http://<npub>.fips:<port>/ WApp URLs and existing window.nostr/exact-origin approvals. Keep persistent FIPS node key separate from user signer, extension-private/Keychain protected, never Flutter/JS/logs. Handle initial start, consent decline, start failure, stop/restart, stale state and reconnection. Build reproducibly for device and simulator, extension signing/provisioning, diagnostic export without secrets. Inspect Apple primary docs for current APIs. Avoid blocking network calls on UI thread and bound memory/queues in extension.

Initial evidence: crates/wmapp-fips-android currently embeds upstream FIPS v0.5.0 rev 80f8f965aa872296edbce84ade9949ece2596602. Native lifecycle and DNS/TUN adapters are references, not portable JNI code. app/lib/src/core/fips_runtime_service.dart lacks iOS implementation; app/ios/Runner/AppDelegate.swift was basic Flutter registration. Read docs/android-fips-first-run-lifecycle.md, docs/deploy/android.md, docs/handoffs/2026-09-01-bundled-fips-wapp-poc.md and current code. Desktop support must remain intact. September 6 TestFlight publication lacked account/certificate/profile, but development installs previously succeeded: recheck exact current signing/device capability and distinguish build vs distribution blockers.

Concurrent work: main was ahead of origin by 3 and untracked docs/fips-tower-bridge-handoff-2026-09-09.md + tools/fips_bridge/ existed. Another worker 082e14f1-9e73-4d5d-b694-8907995e06a7 owns WMAPP native Tower bridge implementation. Do not overwrite or duplicate it. Read that handoff and current source. Separate iOS runtime from the bridge; preserve Flight Deck browser origin, secure context, Dexie cache and sync worker ownership. Existing bridge contract window.wingmanTowerTransport.connect({endpoint,logicalTower}) returns {version:1,endpoint,proxyBaseUrl}; do not alter it. Direct .fips WApp navigation is permitted by the accepted WApp PoC; do not move embedded Flight Deck/Tower origin to .fips. The older bridge note that iPhone stays HTTPS/unsupported reflects missing runtime at that time; this task now adds that runtime, but do not claim unrelated Tower bridge iOS capability without testing.

Architecture pre-change: resolve latest version at /Users/mini/code/wingmanbefree/artifact-wapp/artifacts/Wingman_Suite/wingman-suite-arch; latest locally is v4 (not v3). Read excalidraw-scene.json (wrapper .scene), including active elements, relationships and embedded images; visually inspect/render where necessary using artifacts skill. Hosted base https://pale-log-tank.rick.runwingman.com/artifacts/Wingman_Suite/wingman-suite-arch/. Keep change in WMAPP and preserve component responsibilities. If material conflict, report precise conflict before consequential change.

Acceptance and validation:
- A real iOS FIPS library/extension builds, is embedded in Runner and connected to Dart controls; not stubs or source-only scaffolding.
- Meaningful tests of Rust FFI ownership/error/start-stop and packet/DNS handling; Swift lifecycle/configuration where practical; Flutter runtime status/start/errors and existing signing-origin regressions. Run cargo tests for changed crates, flutter analyze/test, iOS simulator build and physical release/archive build as toolchains permit. Validate Android build/tests if shared code changes.
- On available signed iPhone: request VPN consent normally, establish authenticated mesh peer, resolve exact .fips URL, load existing WApp and Nostr login while ordinary HTTPS/DNS work. Never claim passed without runtime evidence. If device UI or signing blocks, finish all independently possible implementation/build/test and document exact remaining steps.
- Exercise/record screen lock/background, Wi-Fi/cellular transition/reconnect, stop/restart, identity persistence, consent refusal and existing-VPN behaviour where feasible. Bound queue and memory; report untested conditions honestly.
- No global trust *.fips, no broad ATS exception added without narrow justification, no public fallback masking mesh failure, no secrets in diagnostics. Bootstrap public test-us01 is PoC only and not production availability.
- Update repo deployment/device test docs with reproducible commands. Return commit(s), files, tests/build results with logs, signed/installed binary status and remaining limitations. Provide task comment milestones after compilation path is confirmed, extension integrated, and validation completes. Manager reviews and handles final thread reply.

Task: @[Build native iPhone FIPS support in WMAPP](mention:task:05cfb9b1-b6d8-42a7-a801-b197a00a78da).
