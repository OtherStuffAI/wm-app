# Refine WMAPP iOS FIPS using Nostr VPN reference

Pete requests implementation now: "can you update with this in mind?" in response to our comparison with https://github.com/mmalmi/nostr-vpn. Update the existing WMAPP native iOS implementation, not just documentation. Keep the existing FIPS protocol/identity/exact .fips WApp behavior; use Nostr VPN as a reference for packet delivery, ownership, lifecycle, network switching and realistic smoke tests. Do not swap FIPS dependencies or import its entire private mesh/enrollment/exit-node product without demonstrated compatibility and explicit need.

Task: @[Build native iPhone FIPS support in WMAPP](mention:task:05cfb9b1-b6d8-42a7-a801-b197a00a78da). Workspace 2e5caefd-dd65-45d2-b747-ee874e8e5fc9; channel d8d00881-ac84-41eb-ab0d-2c2afb77ddf3; scope 76d518f7-c477-4374-bf74-5d36fda570ed; thread 3d7d10a6-14b9-4c56-a52b-c946a96eb5c2. @[Refinement authorization](mention:message:76ffd13e-7cfc-4b48-b32b-210af3e4f374), @[Nostr VPN reference](mention:message:eed746ec-e409-4600-a575-7cdee43f4dc8), @[iPhone deployment authorization](mention:message:593d959c-e97b-4308-9f50-181fd701c0fc). Read full task/latest comments and current originating thread at start and before handoff. Manager def818b3-6b7a-4382-85f1-cc2a8652c717 owns chat; you post task milestones/evidence and return supervised callback. Keep task in_progress until manager accepts. Use broker MCP or import pg(method,path,body) from /Users/mini/wingmen/wingman21/mycode/fips-delivery-pg.mjs (workspace explicit, do not use its default channel/thread constants). Worker may lack MCP dispatch context; helper provides signed typed PG routes. No raw secret fallback. Set own goal/reflect if permitted, stop at handoff.

Work/write scope /Users/mini/code/wm/wmapp on main. Current HEAD 6d5db44; implementation was committed in 9c4caa9 with concurrent Flight Deck 1915 bundle. Read docs/deploy/ios-fips.md and docs/handoffs/2026-09-09-ios-fips-validation.md, then all native implementation. Pre-existing untracked docs/fips-tower-bridge-handoff-2026-09-09.md and tools/fips_bridge/ belong to independent reviewer; preserve. Inspect full worktree before edits/commit, preserve concurrent work, commit all compatible nonignored tested state, no destructive git or history rewrite. Avoid shared-index race: inspect staged files immediately before commit; report exact commit containing your changes. Do not edit Flight Deck/Tower/Autopilot or change bridge contracts/browser origins. No Autopilot restart. Ordinary builds and in-place install/launch on Pete's connected known iPhone are authorized when signing allows; no uninstall/data clearing, TestFlight/App Store publication or account/team substitution.

Reference inspected by manager at mmalmi/nostr-vpn commit 7926f28843c24ceed55863c7511d798534436720 (GitHub mirror; canonical git.iris.to). Read source directly, verify revision/current differences as appropriate, record what informs implementation. Useful files: ios/PacketTunnel/PacketFlowBridge.swift, PacketFlowBridgeLifecycle.swift, PacketTunnelProvider.swift; ios/Sources/PacketTunnelController.swift and PacketTunnelReplacementTransaction.swift; ios/README.md; mobile smoke docs/scripts; crates/nostr-vpn-app-core native packet-flow API. They use NEPacketTunnelFlow and Rust JSON C ABI with callback-delivered packets, explicit context release, generation tracking and underlay monitors. Root Cargo.toml uses nvpn-fips-core/nvpn-fips-endpoint 0.4.72; ours is patched isolated jmcorgan/fips 0.5.0 rev 80f8f965aa872296edbce84ade9949ece2596602. License MIT: if adapting actual source retain required attribution/license notices, otherwise implement concepts in our own boundaries.

Required outcomes:
1. Replace the extension's 10 ms/100 Hz output-drain timer with bounded event-driven packet delivery/wakeup. Prefer Rust delivery callbacks (or equivalent native readiness) with batching and backpressure. No unbounded DispatchQueue tasks/packets; explicit byte/packet queue limits. Idle extension should not wake 100 times/second just to check an empty queue. Lifecycle health check can run separately at sensible low frequency.
2. Make FFI/context lifetime and teardown robust: exactly-once context release across start failures and stop; no callbacks after teardown, no use-after-free, no callback-to-stop deadlock, no stale-generation deliveries. Preserve one outstanding packetFlow read across restart, and handle rejected writes/runtime death honestly. Validate stop during startup, path switches and repeated restart races through meaningful tests.
3. Review/adapt reference network-switch handling and manager/controller replacement sequencing where it exposes concrete gaps in our NWPathMonitor/generation code. Keep public iOS APIs, existing Keychain node identity, split fd00::/8 + .fips DNS, public network preservation, exact-origin signer trust, scoped ATS and no silent public fallback. Preserve prompt/cancel/retry behavior.
4. Add reproducible idle and active packet-path/lifecycle smoke evidence. Measure idle wakeups/callbacks or CPU on a supported host harness; do not label that iPhone battery data. Test bounded packet batching/backpressure and ownership failures. Build artifacts must contain updated real runtime. Add source-reference comparison explaining adopted patterns and deliberate differences; don't claim Nostr VPN interoperability without a real proof.
5. Run appropriate final Rust/native lifecycle tests, live authenticated bootstrap + packet DNS test, Swift tests, Flutter analyze/full tests, simulator build and unsigned device/archive. Rebuild all affected XCFramework architectures pinned Rust 1.98.0. Android tests/build only if shared code changes or regression risk. Tests passed prior: 173 Flutter; 8 iOS Rust plus separate network test; Swift settings; Android APK/Gradle, Rust13 serial (parallel FD reuse race documented). Do not repeat unrelated broad suites without change-based reason.

Known constraints: final signing check September9 reported No Accounts and Runner/extension profiles missing Network Extensions capability on team N5DRUM6S94. Historical Sept6 success does not override current failure; recheck after independent work, use authorized account/provisioning if restored. Peter's iPhone15 Pro id 8A1C111C-F340-5C1A-B609-B022E9B7D832 was connected; reverify. Never install unsigned binaries. If signing blocks, deliver all build/test improvements with exact error and required remaining user step. Simulator install/process launch previously succeeded but rendered UI was white due macOS WMAPP owning fixed port127.0.0.1:47831. Do not disrupt desktop WMAPP or change browser origin just to hide this; use isolated native harness to validate runtime and clearly distinguish simulator UI/device VPN limits. Physical WApp/Nostr login, ordinary HTTPS/DNS during VPN, lock/network transitions, memory/battery remain acceptance items when signed device works.

Architecture: resolve latest local Wingman_Suite/wingman-suite-arch version (last confirmed v4) under /Users/mini/code/wingmanbefree/artifact-wapp/artifacts before consequential changes; read .scene wrapper relationships in excalidraw-scene.json, preserving TowerSyncService/Dexie/browser boundaries. Hosted reference https://pale-log-tank.rick.runwingman.com/artifacts/Wingman_Suite/wingman-suite-arch/. This refinement is local to WMAPP native runtime; no suite responsibility changes needed.

Report task milestones after reference comparison/implementation choice, packet/lifecycle change and tests/build. Final callback: source commits, adopted changes, measured evidence (idle/active), tests/builds, artifact hashes, exact signing/device state and remaining limits. Update docs with reproducible validation. Manager independently reviews then moves task to review and replies to Pete.
