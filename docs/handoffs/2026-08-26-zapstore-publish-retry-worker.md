# WMAPP Zapstore publication retry

Resume and complete Flight Deck task @[Publish signed WMAPP Android release to Zapstore](mention:task:4cccad9c-82a0-4723-a6c5-66190cc51faf) after Pete restarted Autopilot with the corrected narrow Nostr capability policy.

Work in `/Users/mini/code/wm/wmapp` on `main`. Read the task and its latest comments first, then read `docs/handoffs/2026-08-26-zapstore-android-release.md` in full. That handoff is the primary technical brief. The originating retry request is @[Message](mention:message:142d5575-be72-4472-9b16-a7572dd23861) in thread `8633e75e-3a33-47d4-9e40-b19bb1aeac43`.

Current state: `main` and `origin/main` are at `3dac278`; the worktree was clean at dispatch. Release `0.1.2+3` is already built and validated, with APK SHA-256 `79d0360a1c429459b5ac753f07441f3cb62f28aef7fed7c8835798ecd2353c2e` and release certificate SHA-256 `6c0da09b9e2375d657645f197419be7b8227a7434e0225512fc760cedf383c8c`. No assets or software events were previously published. A post-restart manager preflight successfully broker-signed kind `32267` as Rick's stable pubkey `ffdc30446bede234446a69124bae94cdbddebf24f0abee4a5cabfc1124313c2e`, proving the original policy blocker is removed.

Your goal is to complete the real publication, not merely test signing:

1. Re-read the live task and latest comments; verify branch/status, APK hash, certificate fingerprint, and public asset absence before mutation.
2. Regenerate the short-lived Blossom upload authorization, upload the APK and icon, and verify both are publicly retrievable with matching hashes/content.
3. Use only this session's broker-backed Nostr signing/publishing tools to sign and publish the exact Zapstore application (`32267`), release (`30063`), and asset (`3063`) templates generated by `zsp`. Never export, search for, print, persist, or pass any nsec, raw key, bunker URI, or capability token. Do not use Pete Tier 2.
4. Preserve the signed event IDs and relay/indexer acceptance evidence. Independently read back the public events and verify their package/version/hash/download fields match the signed APK.
5. Verify the usable Zapstore listing/install path or tested in-app search instructions. Separate catalog/download proof from device-install proof if no device is available.
6. Update the durable handoff with final evidence. Preserve concurrent work; do not reset/rebase/force-push. Commit all nonignored tested state on `main` and push it, excluding build artifacts and secrets.
7. Post concise progress and final evidence to task `4cccad9c-82a0-4723-a6c5-66190cc51faf`, move it to `review` only after public verification, and report the originating message/thread reference. Do not post directly into Pete's chat thread; the manager session will relay milestones.

If a capability is still denied, capture the exact operation, event kind, route/server scope, and error once; verify request scoping; then stop at the narrow blocker without raw-key workarounds. Do not restart Autopilot or any service.
