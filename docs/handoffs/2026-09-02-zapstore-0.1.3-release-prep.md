# WMAPP Android 0.1.3 Zapstore release preparation

## Goal

Prepare the current Android/FIPS WMAPP state as a reproducible, signed Zapstore release input. Do not publish, upload, request a Nostr signature, expose signing secrets, or restart any registered WApp.

## Current state

- Work in `/Users/mini/code/wm/wmapp` on `main`.
- Current source head begins at `1b48bad` and includes the Android embedded FIPS `VpnService` work plus DNS and lifecycle hardening.
- `app/pubspec.yaml` is still `0.1.2+3`.
- The existing generated APK is 34,700,794 bytes with SHA-256 `9c178aae324df3c3ae09d88c80cfadf2744c9766b23cbcae3c1c1955e0b4a5a4`, but its manifest still says `0.1.2` / code `3`.
- The established Android release certificate SHA-256 is expected to remain `6c0da09b9e2375d657645f197419be7b8227a7434e0225512fc760cedf383c8c`. Verify this from the rebuilt APK; do not silently accept a different certificate.
- The publisher consumes fixed canonical inputs from this repo: `zapstore.yaml`, `app/build/app/outputs/flutter-apk/app-release.apk`, `app/build/app/outputs/flutter-apk/app-release_icon.png`, and the versioned release-notes file.

## Required changes

1. Bump WMAPP to `0.1.3+4`.
2. Add `docs/release-notes/0.1.3.md`, concisely covering the embedded Android FIPS VPN interface, lifecycle hardening, public-DNS preservation, and FIPS WApp access. Do not promise iOS support.
3. Update `zapstore.yaml` to point at the `0.1.3` release notes while keeping Pete's publisher npub, package identity, repo, and artifact path unchanged.
4. Build the signed Android release using the existing `build_android_release.sh` and durable signing setup. Never print, copy, search for, or commit key material or passwords.
5. Ensure `app-release_icon.png` remains the intended canonical icon. Regenerate only through the repo's documented Zapstore tooling if required.
6. Reconcile `docs/deploy/android.md` so the release handoff uses the owner-only WMAPP Zapstore Publisher WApp with Pete's NIP-07 browser signature. Remove any conflicting suggestion that Rick should publish/sign through a backend or MCP route. Keep raw-key prohibition explicit.

## Validation

- Flutter tests/static checks appropriate to the changed metadata and release build pass.
- APK package is exactly `com.wingmanbefree.wingman_app`, version name `0.1.3`, version code `4`, min SDK `24`, target SDK `36`.
- `apksigner verify --verbose --print-certs` passes using a discoverable JDK (Android Studio's bundled JBR is acceptable) and the signer certificate SHA-256 exactly matches the established fingerprint above.
- APK contains the ARM64 embedded FIPS native library and the Android VPN service declarations/permissions.
- `zsp publish --check zapstore.yaml` passes if the installed `zsp` supports this current config. If tooling is unavailable or the command is incompatible, record exact evidence and still complete every local validation possible.
- Report final APK/icon sizes and SHA-256 hashes for the publisher pin update.

## Git and handoff

- Preserve concurrent work and inspect the full worktree before committing.
- Commit all nonignored tested state on `main` using Conventional Commits and push `origin/main`.
- Do not publish to Zapstore and do not restart the publisher WApp.
- Return the commit, push evidence, validation results, certificate fingerprint, APK/icon hashes and sizes, and any blocker.
